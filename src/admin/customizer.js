document.addEventListener('DOMContentLoaded', function() {
   console.log('customizer.js loaded');


   // const stl = document.createElement('style');
   // stl.innerHTML = `
   //    ast-single-palette-wrap
   //
   // `;
   //
   // setTimeout(() => {
   //    const colorPaletteContainer = document.querySelector('.ast-color-palette-container');
   //    console.warn(colorPaletteContainer);
   // }, 500);
});
